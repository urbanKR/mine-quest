<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml" fx:controller="application.Controller"
            stylesheets="@application.css"
            style="-fx-background-color: #f0f4f7;">

    <!-- Top bar: fixed at top -->
    <top>
        <HBox alignment="CENTER" spacing="20"
              style="-fx-background-color: rgba(230,238,245,0.7); -fx-padding: 10; -fx-background-radius: 15; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 10, 0, 0, 2);">

            <VBox spacing="5" alignment="CENTER_LEFT">
                <Label text="Health"/>
                <ProgressBar fx:id="healthBar" progress="1.0" prefWidth="150"/>
            </VBox>

            <VBox spacing="5" alignment="CENTER_LEFT">
                <Label text="Oxygen"/>
                <ProgressBar fx:id="oxygenBar" progress="1.0" prefWidth="150"/>
            </VBox>

            <Region HBox.hgrow="ALWAYS"/>

            <HBox spacing="5" alignment="CENTER">
                <Label text="🪙" style="-fx-font-size: 16"/>
                <Label fx:id="coinsLabel" text="000"/>
            </HBox>

            <HBox spacing="5" alignment="CENTER">
                <Label text="💣" style="-fx-font-size: 16"/>
                <Label fx:id="bombsLabel" text="000"/>
            </HBox>

            <HBox spacing="5" alignment="CENTER">
                <Label text="🔥" style="-fx-font-size: 16"/>
                <Label fx:id="torchesLabel" text="000"/>
            </HBox>

        </HBox>
    </top>

    <!-- Grid in the center -->
    <center>
        <GridPane fx:id="gridPane" style="-fx-background-color: #bdc3c7;" />
    </center>

</BorderPane>

